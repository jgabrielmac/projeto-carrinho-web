import { makeStyles } from '@material-ui/core/styles';

export const styles = makeStyles(() => ({
  inputContainer: {
    width: 900,
    backgroundColor: 'white',
  },
  rightIcon: {
    marginLeft: 10,
  },
}));
